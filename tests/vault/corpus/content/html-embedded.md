# HTML Embedded

<div class="note-box">
  <p>Raw HTML block with a <strong>bold</strong> word.</p>
</div>

Inline HTML: this word is <em>emphasized</em> via a tag, not Markdown syntax.

<!-- an HTML comment, should not be treated as content -->
