# Benchmarks

Specified in [docs/testing/test-vault-spec.md §9](../../../docs/testing/test-vault-spec.md#9-benchmark-tiers). Fixed scenarios and thresholds for tiers S/M/L, run against vaults produced by `generators/scale/`.

Thresholds are set once a first implementation exists — a threshold invented before any measurement is noise. Tiers, seeds (`lith-s-1`, `lith-m-1`, `lith-l-1`), and measured operations are fixed now; named configuration profiles per tier land alongside the generator in M1.

No results here yet — implementation lands in M1.
